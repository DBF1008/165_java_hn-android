package com.manuelmaly.hn.prefs;

/**
 * The reading font-size options offered in settings.
 *
 * <p>Pure (no Android dependencies) so the preference semantics can be unit-tested directly.
 */
public enum FontSize {
    SMALL, NORMAL, BIG
}
